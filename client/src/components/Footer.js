import React from "react";

function Footer(){
    return(
        <nav class="navbar fixed-bottom navbar-dark bg-dark" style={{position: "fixed", marginTop: "0"}}>
        <a class="navbar-brand" href="#">Fixed bottom</a>
        </nav>
    )
}

export default Footer;